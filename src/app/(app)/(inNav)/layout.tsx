import type { FCC } from "@/types/react";
import { NavLayout } from "@/components/nav/NavLayout/NavLayout";

const InNavLayout: FCC = ({ children }) => <NavLayout>{children}</NavLayout>;

export default InNavLayout;
